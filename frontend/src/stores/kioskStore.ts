/**
 * Zustand store for kiosk frontend state machine.
 * Manages the 6-state kiosk flow: idle, payment, capture, processing, reveal, reset.
 * Mirrors the backend session state with optimistic UI updates.
 */
