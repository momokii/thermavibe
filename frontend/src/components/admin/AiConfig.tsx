/**
 * AI configuration panel — provider selection, API key input, and system prompt editor.
 * Allows operator to switch AI providers and customize the reading prompt.
 */
export default function AiConfig() {
  return <div className="ai-config" />
}
