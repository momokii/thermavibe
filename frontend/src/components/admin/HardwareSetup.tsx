/**
 * Hardware setup panel — camera and printer configuration.
 * Allows operator to select active webcam and bind thermal printer.
 */
export default function HardwareSetup() {
  return <div className="hardware-setup" />
}
