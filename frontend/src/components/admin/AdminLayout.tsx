/**
 * Admin dashboard layout wrapper — PIN-protected.
 * Provides navigation sidebar and authentication guard.
 */
export default function AdminLayout() {
  return <div className="admin-layout" />
}
