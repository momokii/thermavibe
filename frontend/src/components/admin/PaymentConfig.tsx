/**
 * Payment configuration panel — gateway setup, pricing, and QRIS settings.
 * Allows operator to enable/disable payment and configure provider credentials.
 */
export default function PaymentConfig() {
  return <div className="payment-config" />
}
