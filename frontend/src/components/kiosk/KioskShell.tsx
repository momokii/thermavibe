/**
 * Kiosk shell wrapper — fullscreen container that manages the kiosk state machine
 * and renders the appropriate screen component for each state.
 * Handles state transitions and error boundaries.
 */
export default function KioskShell() {
  return <div className="kiosk-shell" />
}
