/**
 * Payment screen — displays dynamic QRIS QR code for the user to scan.
 * Polls backend for payment confirmation. Skipped when payment is disabled.
 */
export default function PaymentScreen() {
  return <div className="payment-screen" />
}
