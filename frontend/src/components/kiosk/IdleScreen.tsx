/**
 * Idle/attract loop screen — the default state when no user is active.
 * Displays looping visual content prompting passersby to interact.
 */
export default function IdleScreen() {
  return <div className="idle-screen" />
}
