/**
 * Reveal screen — displays the AI-generated "Vibe Reading" text.
 * Also triggers the thermal printer output. Shows for a configurable duration
 * before auto-resetting to idle.
 */
export default function RevealScreen() {
  return <div className="reveal-screen" />
}
