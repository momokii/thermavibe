/**
 * Capture screen — shows live camera feed with a 3-second countdown overlay.
 * Triggers photo capture when the countdown reaches zero.
 */
export default function CaptureScreen() {
  return <div className="capture-screen" />
}
