/**
 * Processing screen — engaging loading animation while AI analyzes the photo.
 * Displays messages like "Reading the stars..." or "Analyzing aesthetic frequencies..."
 */
export default function ProcessingScreen() {
  return <div className="processing-screen" />
}
