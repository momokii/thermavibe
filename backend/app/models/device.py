"""Device configuration ORM model."""

# Device model fields:
# - id: UUID primary key
# - device_type: Enum (camera, printer)
# - name: String
# - vendor_id: String
# - product_id: String
# - capabilities: JSONB
# - is_active: Boolean (default True)
# - last_seen_at: DateTime (nullable)
