"""Configuration CRUD service."""

# Responsibilities:
# - Read config from DB (OperatorConfig model)
# - Update config by category
# - Validate config values before persistence
# - Apply config changes at runtime
